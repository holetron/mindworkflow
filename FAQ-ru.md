# Часто Задаваемые Вопросы (FAQ) - Git

## 🤔 Основные вопросы

### Q: Я скачал Git, но ничего не происходит при запуске
**A:** Git - это инструмент командной строки. После установки:
1. Откройте командную строку (Windows: Win+R → cmd)
2. Введите `git --version`
3. Если появилась версия - Git работает!

### Q: Пишет "git не является внутренней командой"
**A:** Git не добавлен в PATH. Решения:
1. Переустановите Git с опцией "Add Git to PATH"
2. Или добавьте путь к Git вручную в переменные среды
3. Перезагрузите командную строку

### Q: Где найти Git после установки?
**A:** Обычные пути установки:
- **Windows:** `C:\Program Files\Git\`
- **macOS:** `/usr/local/bin/git`
- **Linux:** `/usr/bin/git`

### Q: Git GUI не открывается
**A:** Попробуйте:
1. Найдите "Git GUI" в меню Пуск
2. Переустановите Git с полным пакетом
3. Используйте альтернативы: GitHub Desktop, VS Code

### Q: Нужен ли антивирус для работы с Git?
**A:** Нет, но антивирус может блокировать Git. Если есть проблемы:
1. Добавьте папку Git в исключения антивируса
2. Временно отключите антивирус при установке

## 🛠️ Проблемы установки

### Q: Какую версию Git скачать?
**A:** Всегда скачивайте с официального сайта:
- **Windows:** https://git-scm.com/download/windows (64-bit версия)
- **macOS:** https://git-scm.com/download/mac
- **Linux:** используйте пакетный менеджер вашего дистрибутива

### Q: Нужны ли права администратора?
**A:** 
- **Windows:** Да, для установки в Program Files
- **macOS/Linux:** Да, для системной установки
- **Альтернатива:** Используйте portable версию без прав администратора

### Q: Git установился, но не работает
**A:** Проверьте:
1. `git --version` в командной строке
2. Переменную PATH (должна содержать путь к Git)
3. Запустите наш диагностический скрипт

## ⚙️ Настройка и конфигурация

### Q: Что делать после установки Git?
**A:** Обязательно настройте:
```bash
git config --global user.name "Ваше Имя"
git config --global user.email "your.email@example.com"
```

### Q: Как сменить редактор по умолчанию?
**A:** Для VS Code:
```bash
git config --global core.editor "code --wait"
```

### Q: Git пишет about LF и CRLF
**A:** Настройте окончания строк:
```bash
# Windows
git config --global core.autocrlf true

# Mac/Linux  
git config --global core.autocrlf input
```

## 🚀 Использование

### Q: Как создать репозиторий?
**A:** 
```bash
mkdir my-project
cd my-project
git init
```

### Q: Как клонировать репозиторий?
**A:**
```bash
git clone https://github.com/user/repo.git
```

### Q: Основные команды для начала работы?
**A:**
```bash
git status       # состояние репозитория
git add file.txt # добавить файл
git commit -m "message" # сохранить изменения
git push         # отправить на сервер
git pull         # получить изменения
```

## 🔧 Решение проблем

### Q: "fatal: not a git repository"
**A:** Вы не в папке с Git репозиторием:
1. Перейдите в папку с репозиторием: `cd /path/to/repo`
2. Или создайте новый: `git init`

### Q: Проблемы с кодировкой (русские символы)
**A:** 
```bash
git config --global core.quotepath false
git config --global gui.encoding utf-8
```

### Q: Git очень медленно работает
**A:** Попробуйте:
1. `git config --global core.preloadindex true`
2. `git config --global core.fscache true`
3. Добавьте папку проекта в исключения антивируса

## 🆘 Экстренная помощь

### Q: Случайно удалил файлы
**A:**
```bash
git checkout HEAD -- filename    # восстановить один файл
git reset --hard HEAD            # восстановить все файлы
```

### Q: Хочу отменить последний commit
**A:**
```bash
git reset --soft HEAD~1   # отменить commit, оставить файлы
git reset --hard HEAD~1   # отменить commit и изменения
```

### Q: Забыл что делал в последний раз
**A:**
```bash
git log --oneline -10     # последние 10 коммитов
git status               # текущее состояние
git diff                 # что изменилось
```

## 🌐 Альтернативы

### Q: Есть ли более простые альтернативы Git?
**A:** Да:
1. **GitHub Desktop** - графический интерфейс
2. **VS Code** - со встроенной поддержкой Git
3. **Веб-интерфейсы** - GitHub.com, GitLab
4. **TortoiseGit** - интеграция с проводником Windows

### Q: Могу ли я использовать Git без командной строки?
**A:** Да! Используйте:
- GitHub Desktop
- VS Code с расширением Git
- IntelliJ IDEA / PyCharm
- Интеграцию в проводнике (TortoiseGit)

---

## 📞 Не нашли ответ?

1. Запустите диагностический скрипт: `git-diagnostic.sh` или `git-diagnostic.bat`
2. Изучите [подробное руководство](git-troubleshooting-ru.md)
3. Создайте [issue](../../issues/new) с описанием проблемы

**Полезные ресурсы:**
- 📖 [Документация Git на русском](https://git-scm.com/book/ru/v2)
- 🎓 [Интерактивный учебник](https://learngitbranching.js.org/?locale=ru_RU)
- 💬 [Сообщество на GitHub](https://github.com/git/git/discussions)