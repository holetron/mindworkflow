# Пошаговая установка Git

## Для Windows

### Способ 1: Официальный установщик (Рекомендуется)
1. **Скачать Git:**
   - Перейдите на https://git-scm.com/download/windows
   - Скачается установщик автоматически (Git-x.x.x-64-bit.exe)

2. **Установка:**
   - Запустите установщик **от имени администратора**
   - Следуйте мастеру установки:
     - ✅ Выберите "Use Git from the command line and also from 3rd-party software"
     - ✅ Выберите "Checkout Windows-style, commit Unix-style line endings"
     - ✅ Выберите "Use Windows' default console window"

3. **Проверка установки:**
   - Откройте командную строку (Win+R → cmd)
   - Выполните: `git --version`
   - Должна появиться версия Git

### Способ 2: Через Chocolatey
```cmd
# Установите Chocolatey, затем:
choco install git
```

### Способ 3: Через Winget
```cmd
winget install --id Git.Git -e --source winget
```

### Способ 4: Portable версия
1. Скачайте с https://git-scm.com/download/windows
2. Выберите "Portable" версию
3. Распакуйте в любую папку
4. Запускайте `git-bash.exe`

## Для macOS

### Способ 1: Homebrew (Рекомендуется)
```bash
# Установите Homebrew если его нет:
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Установите Git:
brew install git
```

### Способ 2: Официальный установщик
1. Перейдите на https://git-scm.com/download/mac
2. Скачайте .dmg файл
3. Откройте и следуйте инструкциям

### Способ 3: Через Xcode Command Line Tools
```bash
xcode-select --install
```

## Для Linux

### Ubuntu/Debian
```bash
sudo apt update
sudo apt install git
```

### CentOS/RHEL/Fedora
```bash
# CentOS/RHEL
sudo yum install git

# Fedora
sudo dnf install git
```

### Arch Linux
```bash
sudo pacman -S git
```

### openSUSE
```bash
sudo zypper install git
```

## Первоначальная настройка

После установки настройте Git:

```bash
# Обязательная настройка
git config --global user.name "Ваше Имя"
git config --global user.email "your.email@example.com"

# Дополнительные настройки (по желанию)
git config --global init.defaultBranch main
git config --global core.editor "code --wait"  # Для VS Code
git config --global color.ui auto
```

## Проверка установки

Выполните диагностику:

### Windows
```cmd
git-diagnostic.bat
```

### Mac/Linux
```bash
./git-diagnostic.sh
```

Или проверьте вручную:
```bash
git --version
git config --list
```

## Частые проблемы и решения

### "git не является внутренней или внешней командой"
- Git не установлен или не добавлен в PATH
- **Решение:** Переустановите Git с опцией "Add Git to PATH"

### "fatal: not a git repository"
- Вы не в Git репозитории
- **Решение:** Выполните `git init` или перейдите в папку с репозиторием

### Проблемы с правами доступа (Windows)
- Запустите командную строку от имени администратора
- Проверьте настройки антивируса

### Git GUI не запускается
- Переустановите Git с полным пакетом
- Или используйте альтернативы: GitHub Desktop, VS Code

## Альтернативные способы использования Git

Если установка не получается:

1. **GitHub Desktop** - https://desktop.github.com/
2. **VS Code** с расширением Git
3. **Веб-интерфейсы:** GitHub.com, GitLab.com
4. **Portable версии** Git

## Получение помощи

- 📖 [Официальная документация](https://git-scm.com/book/ru/v2)
- 🔍 [Подробное руководство по устранению неполадок](git-troubleshooting-ru.md)  
- 🛠️ [Запустите диагностический скрипт](git-diagnostic.sh)

Если проблема не решается, создайте issue с описанием:
- Операционная система и версия
- Попытки установки
- Вывод команды `git --version`
- Сообщения об ошибках