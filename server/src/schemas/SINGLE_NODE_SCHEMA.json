{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SINGLE_NODE_SCHEMA",
  "type": "object",
  "required": ["type", "title", "content"],
  "properties": {
    "type": {
      "type": "string",
      "const": "text",
      "description": "Node type, always text for simple responses"
    },
    "title": {
      "type": "string",
      "description": "Short title for the response node",
      "maxLength": 100
    },
    "content": {
      "type": "string",
      "description": "Main AI agent response"
    }
  },
  "additionalProperties": false,
  "description": "Schema for creating a single text node with an AI agent response. Used when a separate node with AI output is needed."
}