# Промпт для GitHub Copilot Coding Agent

## Приоритетные задачи для исправления UX проблем LocalCreativeFlow

### 1. 🎯 Исправить размеры узлов и синхронизацию фона (КРИТИЧНО)
- **Проблема**: Синий фон (border) узла не соответствует реальному размеру узла
- **Требуется**: Сопоставить размеры FlowNodeCard контейнера с его фоновым окном
- **Файлы**: `/app/src/features/nodes/FlowNodeCard.tsx`
- **Цель**: Убрать несоответствие между размером узла и его границами

### 2. 🎨 Исправить логику изменения цвета узлов (КРИТИЧНО)
- **Проблема**: Изменение цвета ноды не применяется корректно
- **Требуется**: Проверить и исправить систему colorTokens и применение цветов
- **Файлы**: `/app/src/features/nodes/FlowNodeCard.tsx`, `/app/src/state/`
- **Цель**: Корректное применение выбранных цветов к узлам

### 3. 🔧 Упростить AI ноды по умолчанию (ВЫСОКИЙ ПРИОРИТЕТ)
- **Проблема**: AI ноды показывают слишком много технических настроек
- **Требуется**: 
  - Свернуть секции поставщика, local stub по умолчанию
  - Переместить кнопку сворачивания правее от настроек в заголовке
  - Оставить только системный промпт и поля из настроек ноды
- **Файлы**: `/app/src/features/nodes/FlowNodeCard.tsx`
- **Цель**: Чистый интерфейс для пользователей

### 4. 🗂️ Создать попапы для настроек (ВЫСОКИЙ ПРИОРИТЕТ)
- **Проблема**: Нижние панели загромождают интерфейс
- **Требуется**:
  - Убрать нижние панели с входящими нодами и настройками
  - Создать модальное окно для этих элементов
  - Добавить кнопку на узле для открытия попапа
- **Файлы**: Новый компонент Modal, обновить GraphCanvas
- **Цель**: Освободить пространство и улучшить навигацию

## Контекст и указания
- Код базируется на React Flow с TypeScript
- Узлы используют динамическую систему размеров с clampHeight
- Система цветов использует colorTokens
- Приложение запускается на порту 5175

## Результат
После выполнения этих задач интерфейс должен стать значительно чище и удобнее для пользователей, с корректным отображением размеров и цветов узлов.